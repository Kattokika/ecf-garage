{{ form_start(form) }}
<div class="row g-3 p-3">
    <div class="col-md-6">
        {{ form_row(form.prenom) }}
    </div>
    <div class="col-md-6">
        {{ form_row(form.nom) }}
    </div>
    <div class="col-md-6">
        {{ form_row(form.poste) }}
    </div>
    <div class="col-md-6">
        {{ form_row(form.email) }}
    </div>
    <div class="d-flex">
        <a href="{{ path('app_users') }}">
            <button type="button" class="btn btn-danger mb-3 me-3">Annuler</button>
        </a>
    {{ form_row(form.submit, {'label': 'Valider'}) }}
    </div>
</div>
{{ form_end(form) }}
