{{ form_start(form) }}
    {{ form_widget(form) }}
    {% if back_button|default(false) %}
        <a href="{{ path('app_avis_index') }}" class="btn btn-danger me-3" style="width: 120px;">Annuler</a>
    {% endif %}
    <button class="btn btn-primary" style="width: 120px;">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
