{% extends 'layout_pro.html.twig' %}

{% block title %}Liste des services{% endblock %}

{% block content %}
<h1 class="text-center p-3">Liste des services</h1>
<div class="d-md-flex justify-content-end">
    <a href="{{ path('') }}" class="btn btn-primary">Ajouter une prestation</a>
</div>
<div>
    <table class="table">
        <thead>
        <tr>
            <th scope="col">Titre</th>
            <th scope="col">Description</th>
            <th scope="col"> </th>
        </tr>
        </thead>
        <tbody>
        <tr>
            <th scope="row">Prestation</th>
            <td class="text-truncate" style="max-width: 150px;">Description de cette prestation un peu longue mais pas tant que ça, faut pas abuser. c'est juste une description, pas un roman</td>
            <td><a href="#" class="btn btn-primary">Modifier</a></td>
        </tr>
        <tr>
            <th scope="row">Prestation</th>
            <td class="text-truncate" style="max-width: 150px;">Description de cette prestation un peu longue mais pas tant que ça, faut pas abuser. c'est juste une description, pas un roman</td>
            <td><a href="#" class="btn btn-primary">Modifier</a></td>
        </tr>
        <tr>
            <th scope="row">Prestation</th>
            <td class="text-truncate" style="max-width: 150px;">Description de cette prestation un peu longue mais pas tant que ça, faut pas abuser. c'est juste une description, pas un roman</td>
            <td><a href="#" class="btn btn-primary">Modifier</a></td>
        </tr>
        </tbody>
    </table>
    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for service in services %}
            <tr>
                <td>{{ service.id }}</td>
                <td>{{ service.nom }}</td>
                <td>{{ service.description }}</td>
                <td>
                    <a href="{{ path('app_service_show', {'id': service.id}) }}">show</a>
                    <a href="{{ path('app_service_edit', {'id': service.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_service_new') }}">Create new</a>
{% endblock %}
