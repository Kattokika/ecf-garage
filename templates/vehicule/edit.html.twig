{% extends 'layout_pro.html.twig' %}

{% block title %}Edit Vehicule{% endblock %}

{% block body %}
    <h1>Edit Vehicule</h1>
    <div class="container">
        <div class="row">
            <div class="col-8">
                {{ include('vehicule/_edit_form.html.twig', {'button_label': 'Update'}) }}

                <a href="{{ path('app_vehicule_index') }}">back to list</a>
                <a href="{{ path('app_vehicule_delete_photos', { slug: vehicule.slug }) }}">Supprimer des photos</a>

                {{ include('vehicule/_delete_form.html.twig') }}
            </div>
        </div>
    </div>

{% endblock %}
